{
  "name": "usage-test",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "rm -rf lib && tsc",
    "bundle": "rm -rf dist && yarn build && webpack && rollup -c"
  },
  "dependencies": {
    "@fluentui/react-icons": "*",
    "@fluentui/react-icons-combined": "*"
  }
}
