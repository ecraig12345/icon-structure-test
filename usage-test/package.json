{
  "name": "usage-test",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "yarn clean && tsc",
    "bundle": "yarn build && webpack && rollup -c",
    "clean": "rm -rf lib dist"
  },
  "dependencies": {
    "@fluentui/react-icons": "*",
    "@fluentui/react-icons-combined": "*"
  }
}
