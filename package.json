{
  "name": "icon-experiments-repo",
  "version": "1.0.0",
  "private": true,
  "description": "Icon experiments",
  "scripts": {
    "build:tests": "yarn workspace tests build",
    "build:usage-test": "yarn workspace usage-test build",
    "build:react-icons": "node ./generateRollup.js",
    "test": "yarn build:react-icons && yarn build:usage-test"
  },
  "author": "",
  "license": "ISC",
  "workspaces": {
    "packages": [
      "usage-test",
      "react-icons",
      "tests"
    ]
  },
  "devDependencies": {
    "@types/react": "^16.0.0",
    "fs-extra": "^10.0.0",
    "glob": "^7.1.7",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "tslib": "^2.1.0",
    "typescript": "~4.1.0"
  }
}
